Copy the following Jar on the target Server <install_directory><group><groupX><instanceX>/ext/lib path
  
  The following Jar must be Add in Runtime dependencies in PS
   -filter-devkit-runtime
   -filter-devkit-studio
   -nimbus-jose-jwt
